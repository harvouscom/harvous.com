# Roundo

Roundo is a paid font from Pangram Pangram (https://pangrampangram.com/products/roundo).

The site loads `Roundo-Variable.woff2` (covers weights 200–700 in a single file). If you only have static weights, swap the `@font-face` block in `site/src/styles/global.css` for per-weight declarations.
