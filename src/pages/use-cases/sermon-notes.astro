---
import UseCaseDetailPage from "../../components/UseCaseDetailPage.astro";
import { getUseCaseBySlug } from "../../lib/use-cases-data.ts";

const useCase = getUseCaseBySlug("sermon-notes");

if (!useCase) {
  throw new Error("Use case not found: sermon-notes");
}
---

<UseCaseDetailPage useCase={useCase} />
