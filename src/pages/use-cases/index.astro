---
import BaseLayout from "../../layouts/BaseLayout.astro";
import PageIntro from "../../components/PageIntro.astro";
import PageClosingCTA from "../../components/PageClosingCTA.astro";
import { fathomSignup } from "../../lib/fathom-events.ts";
import { Icon } from "astro-icon/components";
import { getUseCases, getUseCaseDisplayTitle } from "../../lib/use-cases-data.ts";

const useCases = getUseCases();
---

<BaseLayout
  title="Use Cases — Harvous"
  description="Harvous works for daily readers, sermon note-takers, small groups, deep book studies, and topical study across all of Scripture."
>
  <!-- Page header -->
  <section class="border-b border-[var(--color-rule)]">
    <PageIntro>
      <span class="eyebrow-pill">Use cases</span>
      <h1 class="mt-3 font-display text-5xl leading-[1.0] md:text-7xl">
        Harvous is for people who actually study.
      </h1>
      <p class="use-cases-hub__lead mt-5 text-lg text-[var(--color-ink-soft)]">
        Not a Bible reader. Not a devotional. A place for your notes, your questions, and everything you want to hold onto from studying Scripture.
      </p>
      <p class="mt-4 text-sm text-[var(--color-ink-soft)]">
        Looking for who you are, not how you study?{" "}
        <a href="/for/" class="font-medium text-[var(--color-accent)] hover:underline">See who Harvous is for</a>.
      </p>
    </PageIntro>
  </section>

  <!-- Use case cards -->
  <section class="mx-auto max-w-5xl px-5 py-16 md:py-20">
    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3" data-reveal="stagger" style="--reveal-delay: 80ms;">
      {useCases.map((uc) => (
        <a
          href={uc.href}
          class="use-case-card group flex flex-col overflow-hidden rounded-2xl border border-[var(--color-rule)] bg-white shadow-[var(--shadow-card)] transition hover:shadow-[var(--shadow-float)] hover:-translate-y-0.5"
        >
          <div
            class="use-case-card__art relative flex aspect-[1.6/1] items-center justify-center"
            style={`--usecase-art-image: url('${uc.image}');`}
          >
            {uc.comingSoon && (
              <span class="absolute right-3 top-3 z-[1] rounded-full border border-[var(--color-rule)] bg-[color-mix(in_oklab,var(--color-card)_88%,transparent)] px-2 py-0.5 text-[0.62rem] font-semibold uppercase tracking-wider text-[var(--color-ink-faint)] backdrop-blur-sm">
                Coming soon
              </span>
            )}
            <span class="use-case-card__badge inline-flex h-11 w-11 items-center justify-center rounded-xl bg-white shadow-[var(--shadow-card)]">
              <Icon name={uc.icon} class="h-5 w-5" style={`color: ${uc.ink};`} />
            </span>
          </div>
          <div class="flex flex-1 flex-col gap-5 p-6">
            <div>
              <h2 class="text-lg font-semibold text-[var(--color-ink)] group-hover:text-[var(--color-accent)]">
                {getUseCaseDisplayTitle(uc)}
              </h2>
              <p class="mt-1.5 text-sm leading-relaxed text-[var(--color-ink-soft)]">
                {uc.tagline}
              </p>
            </div>
            <div class="mt-auto flex items-center gap-1 text-sm font-medium text-[var(--color-accent)]">
              {uc.comingSoon ? "See how it will work" : "See how it works"}
              <svg class="h-3.5 w-3.5 transition-transform group-hover:translate-x-0.5" viewBox="0 0 16 16" fill="currentColor">
                <path fill-rule="evenodd" d="M2 8a.75.75 0 0 1 .75-.75h8.69L8.22 4.03a.75.75 0 0 1 1.06-1.06l4.5 4.5a.75.75 0 0 1 0 1.06l-4.5 4.5a.75.75 0 0 1-1.06-1.06l3.22-3.22H2.75A.75.75 0 0 1 2 8Z" clip-rule="evenodd"/>
              </svg>
            </div>
          </div>
        </a>
      ))}
    </div>
  </section>

  <PageClosingCTA
    heading="Whatever way you study, there's a place for it here."
    fathomTrack={fathomSignup.useCasesHub}
  />
</BaseLayout>

<style>
  .use-cases-hub__lead {
    max-width: 28rem;
    margin-inline: auto;
  }

  @import "../../styles/use-cases.css";

  .use-case-card__art {
    background-color: var(--color-warm);
    background-image: var(--usecase-art-image);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }
  .use-case-card__art::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      color-mix(in oklab, var(--color-paper) 12%, transparent) 0%,
      color-mix(in oklab, var(--color-ink) 8%, transparent) 100%
    );
    pointer-events: none;
  }
  .use-case-card__badge {
    position: relative;
    z-index: 1;
  }
</style>
