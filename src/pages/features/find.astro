---
import FeatureCategoryPage from "../../components/FeatureCategoryPage.astro";
import { getFeatureCategoryBySlug } from "../../lib/feature-categories-data.ts";

const category = getFeatureCategoryBySlug("find");

if (!category) {
  throw new Error("Feature category not found: find");
}
---

<FeatureCategoryPage category={category} />
